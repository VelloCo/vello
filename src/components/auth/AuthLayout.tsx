import { Logo } from '../Logo';
import { motion } from 'framer-motion';
import { appPath } from '../../lib/paths';

export function AuthLayout({ children }: { children: React.ReactNode }) {
  return <main className="relative min-h-screen overflow-hidden bg-paper"><section className="relative z-10 mx-auto flex min-h-screen max-w-[560px] flex-col px-5 py-7 pb-32 sm:px-10 lg:py-10 lg:pb-40"><motion.a href={appPath('/')} aria-label="Voltar para a Vello" initial={{ opacity: 0, transform: 'translateY(-8px)' }} animate={{ opacity: 1, transform: 'translateY(0)' }} transition={{ duration: 0.28, ease: [0.23, 1, 0.32, 1] }}><Logo /></motion.a><motion.div className="mx-auto flex w-full max-w-[430px] flex-1 items-center py-12" initial={{ opacity: 0, transform: 'translateY(16px)', filter: 'blur(4px)' }} animate={{ opacity: 1, transform: 'translateY(0)', filter: 'blur(0px)' }} transition={{ duration: 0.42, delay: 0.06, ease: [0.23, 1, 0.32, 1] }}>{children}</motion.div></section><motion.div className="pointer-events-none absolute inset-x-0 -bottom-1 z-0 h-32 text-ink sm:h-40" initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.55, delay: 0.12, ease: [0.23, 1, 0.32, 1] }}><svg className="absolute inset-0 h-full w-full" viewBox="0 0 600 160" preserveAspectRatio="none" aria-hidden="true"><path d="M0 78 C90 12 170 20 255 66 C350 118 423 122 496 58 C540 20 570 18 600 38 V160 H0 Z" fill="currentColor" /></svg></motion.div></main>;
}
